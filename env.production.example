# Production Environment Variables for MentraFlow Frontend
# Copy this file to .env and update with your production values
# 
# Note: Vite uses VITE_ prefix for environment variables

# Backend API URL
# Since nginx proxies /api/* to backend, use the same domain as frontend
# The frontend will make API calls to /api/* which nginx will proxy to backend
# Use https:// once SSL is configured, http:// for initial setup
VITE_BACKEND_URL=https://getmentraflow.com

# Google OAuth Client ID
# Get this from Google Cloud Console: https://console.cloud.google.com/
# Make sure to add your domain to:
# - Authorized JavaScript origins: 
#   - http://localhost:3000 (for development)
#   - https://getmentraflow.com (for production)
#   - http://getmentraflow.com (if not using SSL yet)
# - Authorized redirect URIs: Same as above
VITE_GOOGLE_CLIENT_ID=your-google-client-id-here.apps.googleusercontent.com

